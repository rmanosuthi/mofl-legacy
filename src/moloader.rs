use std::path::PathBuf;
use std::fs::File;
use std::io::prelude::*;

impl moloader {
    fn extract(file: PathBuf) -> momod {

    }
    fn sanitize(mod: momod) -> bool {

    }
    fn check_sanity(mod: momod) -> bool {

    }
}